/// Stub de notificaciones. Expand with firebase_messaging +
/// flutter_local_notifications when push notifications are needed.
class NotificationService {
  Future<void> sendSimpleNotification(String title, String body) async {
    // No-op until FCM is configured.
  }
}
